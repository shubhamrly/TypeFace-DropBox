import React from 'react';
import { Typography } from '@mui/material';

function Footer() {
  return (
    <footer style={{ textAlign: 'center', padding: '16px', background: '#f9f9f9', marginTop: '32px' }}>
      <Typography variant="body2" style={{ color: '#888' }}>
        Sample for assignment, Edu use only.
      </Typography>
    </footer>
  );
}

export default Footer;
